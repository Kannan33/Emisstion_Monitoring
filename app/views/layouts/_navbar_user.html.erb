<nav class="navbar navbar-expand-md navbar-expand-lg bg-body-tertiary sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">Emission Monitoring System</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Vehicles", vehicles_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= edit_user_registration_path %>">Edit Profile</a>
          </li>
          <li class="nav-item">
            <%= link_to new_vehicle_path, class: "btn btn-sm btn-primary" do %>
              <i class="bi bi-plus-lg"> Add Vehicle</i>
            <% end %>
          </li>
        <% else %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Login
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "User Login", new_user_session_path, class: "dropdown-item" %></li>
              <li><%= link_to "RTO Login", new_rto_officer_session_path, class: "dropdown-item" %></li>
              <li><%= link_to "Admin Register", new_rto_officer_registration_path, class: "dropdown-item" %></li>
              <li><%= link_to "Register", new_user_registration_path, class: "dropdown-item" %></li>
            </ul>
          </li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <div class=" ms-auto">
          <%= link_to "sign out", destroy_user_session_path, data: { turbo_method: :delete}, class: "btn btn-outline-danger" %>
        </div>
      <% end %>
    </div>
  </div>
</nav>